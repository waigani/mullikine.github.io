import codelingo/ast/go

go.file(depth = any):
  go.decls:
    go.func_decl:
      go.ident:
        name == "SayA"
      go.func_type:
        go.field_list
        go.field_list:
          go.field:
            go.ident:
              name == "error"
